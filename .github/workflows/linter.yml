name: lint-check
on: push
jobs:
  unit_test-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
              cd frontend
              make install
              make build
              make lint
